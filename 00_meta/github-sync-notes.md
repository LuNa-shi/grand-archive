# GitHub Sync Notes

This vault is now a git repository.

Suggested next step:
- add a GitHub remote
- push the initial commit
- then keep syncing via git

Recommended commands:
```bash
git remote add origin https://github.com/<your-user>/<your-repo>.git
git branch -M main
git push -u origin main
```
